import 'package:flutter/material.dart';

class CallRoom extends StatelessWidget {
  const CallRoom({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Call room")));
  }
}
